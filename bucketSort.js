// Formulate an approach to perform bucket sort and track the number of elements in each bucket.

const BucketSort = (input) => {
  const n = input.length;
};

console.log(BucketSort([2, -1, 5, 11, 4, 3, 2])); // [-1, 2, 2, 3, 4, 5, 11]
